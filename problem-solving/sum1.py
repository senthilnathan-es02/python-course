# Palindrome Checker
# Write a program to check if a given string is a palindrome. A palindrome reads the same backward as forward.
# Example:
# Input: "madam"
# Output: True.

user = str(input())
for i in user:
    if user=="madam":
        print(True)
        break
    else:
        print(False)
        break
    
    
     

